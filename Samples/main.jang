julio wants int main(int args)
{
    julio says "Hello, World!";

    make int x be 2;
    make int y be 4;

    change x to y * 2;

    make int arr[];

    change arr[4] to 42 + x;

    make int i be 0;
    till (i > 10) is fr
    {
        julio says i;
    }

    julio gets 0;
}