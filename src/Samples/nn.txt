series of inputs get fed into the input layer.
feed inputs into first layer input array
    Input array is an array for arrays
    Index in input array corresponds to perceptron array indices

Program structure:
Perceptron class
- ReLU Activation
    Takes input sum as input
- Input Summation
    Takes input array as input
    Outputs input sum

Dense hidden layer class
- Constructor
- Manual weight setting
- Feed into perceptron layer

Dense input layer class
- Create input layer to next 

Dense output layer class